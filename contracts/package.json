{
  "name": "empowerchain-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for EmpowerChain Finance",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:amoy": "hardhat run scripts/deploy.js --network polygonAmoy",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network polygon"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^17.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {}
}
